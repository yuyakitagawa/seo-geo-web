// 記事生成プロンプトの共通部分。ニュース記事（generate.ts）とHOW TO記事（generate-howto.ts）で
// 媒体の性格・図解・文体をずらさないため、共有する指示はここ1か所だけに置く。
import { LESSONS } from "../src/lib/curriculum";
import { getAllArticles } from "../src/lib/content";
import { indexableArticles } from "../src/lib/indexability";

export const MEDIA_INTRO = `あなたは日本語のSEO/GEO専門メディアの編集者です。読者は事業会社・制作会社でSEO/GEOを担当している実務者で、
「自社サイトのどこが動くのか」「何をすればいいのか」を知るために読みます。`;

export const FIGURE_RULES = `# 図解（必ず3〜4個入れる）
本文には次のコンポーネントをMDXとしてそのまま書ける（importは書かない）。文章だけの記事にしない。
- 「## やること／やらなくていいこと」の箇条書きは、リストの代わりに必ず FigureDoDont で書く。
  項目は短い1文にし、理由の説明は図の直後の段落に書く。
- 残りは内容に合わせて選ぶ: 比較→FigureCompare、手順・時系列→FigureFlow、
  増減や複数項目の大小→FigureBars、単独の数値の並び→FigureStats、公式発表の一文→FigureQuote。
- **FigureQuote の text は出典に載っている一文をそのまま写す**。要約・言い換え・2文の合成・
  自分で作った一文を入れてはいけない（引用として出す以上、原文と1字も違ってはいけない）。
  出典が英語なら英語のまま写し、日本語の意味は図の直後の段落に書く。
  そのまま写せる一文が無いなら FigureQuote を使わず、別の図か地の文にする。
  source には発表元・日付・発言者を書き、出典ページの実際のタイトルを使う（自分でタイトルを作らない）。
- 同じ種類の図を連続させない。図は該当セクションの直後に置く。
- 図の中身は本文と同様、元記事にある事実だけ。属性の文字列内ではダブルクォートを使わず「」を使う。
- JSXブロックの前後には空行を入れる。

<FigureDoDont
  title="（内容が分かる具体的なタイトル）"
  dos={["やること1", "やること2"]}
  donts={["やらなくていいこと1"]}
/>

<FigureCompare
  title="..."
  caption="（任意）"
  cols={[
    { label: "A", tone: "seo", sub: "（任意）", points: ["...", "..."] },
    { label: "B", tone: "geo", points: ["..."] },
  ]}
/>
（tone は seo | geo | news | accent。2〜3カラム）

<FigureFlow title="..." steps={[{ label: "...", desc: "（任意）" }]} />

<FigureStats title="..." stats={[{ value: "40%", label: "何の数字か", note: "（任意）" }]} />

<FigureBars title="..." unit="%" caption="（任意）" bars={[{ label: "何の値か", value: 40, note: "（任意）" }]} />

<FigureQuote text="（公式発表の一文。原文が英語ならそのまま）" source="（発表元・日付・発言者）" />

属性名は上のとおりに書く（別名にすると何も表示されない）。`;

// 「薄い要約」を防ぐ内容の厚みの要件。AdSense審査・E-E-A-Tの観点で、要約に独自価値を足す。
export const DEPTH_RULES = `# 内容の厚み（すべて満たす）
- **前提の説明**: 専門用語・機能名は初出時に1文で説明する（初心者が読み始めても迷子にならない）。
- **手順の具体性**: 「やること」は抽象論で終わらせず、どの画面・どのファイル・どの設定を触るかまで書く。
- **具体例2つ以上**: 日本の業種の例を、数字や画面の文言レベルまで具体化する（架空のサイト名でよいが、
  仮の数値は「例えば」と明示する。元記事の数値と混ざらないようにする）。
- **判断基準**: 読者が「自分は対応すべきか」を判断できる分岐（サイト規模・業種・依存度）を入れる。
- 元記事・出典を読んだだけでは書けない内容（日本市場への翻訳・手順化・判断基準）が全体の半分以上を占めること。`;

// 断定の質。手で書いた記事（0067/0068）にあって自動生成の記事に無かったのがここだった。
// 出典の格を書き分け、数字に分母を付け、自分の主張の限界を自分で1文書く。
export const CREDIBILITY_RULES = `# 断定の質（すべて満たす）
- **出典の格を書き分ける**: 公式ドキュメント・公式ブログか、第三者の調査・実験か、個人の観測かを本文で区別する。
  調査会社やメディアの独自調査を根拠にするときは、査読を経た研究ではないと分かるように書く
  （図の caption か本文の1文に入れる。公式発表と同じ強さで書かない）。
- **数字には分母と条件を付ける**: 「40%」だけで書かない。「50回答中20回答」のように母数を書き、
  調査時期・対象・実行環境のうち結果を左右するものを添える。
- **数字を主張の根拠に使う**: FigureStats に入れて終わりにしない。その数字から何が言えるのかを
  地の文で書く（例:「71回答中69回答に出たが、最初に挙げられたのは25回答。出現しやすさと順位は別の指標」）。
- **主張の限界を自分で書く**: 数字が確定値でないこと・主張が成り立たない条件・施策が結果を保証しないことを、
  該当箇所に1文で書く。自サイトのツールや手順を勧めた直後には、それでも分からないままのことを続けて書く。
- ただし全文を留保で埋めない。「## 結論」の1文目は断定のままにし、限界は本文の該当箇所に置く。`;

/** 2段階生成の2回目（編集長レビュー）で使う。草稿を検査して改稿した完成稿だけを出力させる */
export const REVIEW_PROMPT = `あなたは同じ媒体の編集長です。直前の草稿を次のチェックリストで検査し、
すべて満たすように改稿した完成稿だけを出力してください。frontmatter付きMDXのみを出力し、
前後に説明文やコードフェンスを付けないこと。問題の無い部分は変えずに維持します。

# チェックリスト
1. 「## 結論」の1文目が、タイトルの問いに答える60字以内の断定文か
2. 本文が薄い要約になっていないか。手順・具体例2つ・判断基準（自分は対応すべきかの分岐）があるか
3. 専門用語が初出で1文説明されているか
4. 「やること」がどの画面・どの設定かまで具体的か
5. 図解が3〜4個あり、種類が偏っていないか。FigureDoDont が「やること／やらなくていいこと」にあるか
6. 元記事・出典に無い数値・固有名詞・機能名が混ざっていないか（あれば削除。「例えば」付きの仮の数値は可）
6-2. FigureQuote の text と、本文中の「」でくくった引用が、出典の原文と1字も違わないか
   （言い換え・要約・合成はすべて不可。写せる一文が無いなら引用をやめる）
7. 禁止表現（「〜が重要です」「本記事では」「まとめると」等）が残っていないか
8. 一文60字以内・1段落3文以内か。**各 ## の直下の最初の段落が、見出しを読まなくても意味が通るか**。
   FAQがあるなら、回答が質問を読まなくても意味が通り、本文の言い換えになっていないか
   （言い換えだけのFAQは節ごと削除する）
9. description が90〜120字でスニペットとして成立するか
10. 書き手個人の経歴・経験談が混ざっていないか
11. 数字に分母と条件が付いているか。図に入れただけで本文の根拠に使われていない数字が無いか
12. 出典が公式発表か第三者の調査かを本文で区別しているか。調査会社の独自調査を公式発表と同じ強さで書いていないか
13. 主張の限界（確定値でない・成り立たない条件・保証しないこと）が1文以上あるか。
   ただし「## 結論」の1文目は断定のままか
14. 「## 結論」に、読者が今日やめるべきことが1つ名指しされているか
15. 太字が記事全体で10箇所以内か。段落まるごとが太字1文の決め台詞になっていないか。
    箇条書きの各項目に、本文と同じことを言う太字ラベルが付いていないか
16. 「Xではなく、Yです」の形で、誰も主張していない否定を置いていないか（読者の思い違いを正す場合だけ残す）。
    「先に〜を書いておきます」のような本題前の前置きが残っていないか。文章の中に装飾の矢印（→）が無いか
17. 内部リンクが本文中に2〜3本あり、すべて「リンク先の一覧」にあるURLか。
   一覧に無いURLへのリンク・「詳しくはこちら」のような文言・同じリンク先の重複があれば直す
   （関連の近いページが一覧に無い場合は、無理に張らず本数を減らしてよい）`;

// 内部リンクの指示。2026-09-09 まで1行も無く、公開73本のうち62本が本文にリンクを1本も持っていなかった。
// 関連記事コンポーネントがあるのでクロール経路は繋がっているが、文脈のあるリンクは生成側でしか作れない。
// **リンク先の一覧は userPrompt 側（linkTargetList）から渡す**。ここは一覧に依存しない規則だけを持つ。
export const INTERNAL_LINK_RULES = `# 内部リンク（本文中に2〜3本）
- **渡された「リンク先の一覧」にあるURLだけ**をリンク先にする。一覧に無いURLを内部リンクにしない（404になる）。
  一覧に関連の近いページが無ければ、無理に張らず本数を減らす（1本でも0本でもよい）。
- 記法は Markdown の \`[文言](/path)\`。文章の流れの中に置く。
- **リンクの文言はリンク先の内容を表す語にする**。「詳しくはこちら」「この記事」のような文言にしない。
- 「## 結論」の1文目には置かない（AI検索と強調スニペットが抜き出す文にリンクを混ぜない）。
- 同じリンク先を2回以上張らない。1つの段落に2本以上入れない。
- 外部の出典URLは本文に再掲しない（frontmatter の sources に書く）。この規則は内部リンクだけの話。`;

/** 生成時にリンク先として提示できるページ。lessons と固定ページは常に、記事は最近のものから。 */
export type LinkTarget = { href: string; label: string };

const FIXED_TARGETS: LinkTarget[] = [
  { href: "/seo", label: "SEO対策とは（カテゴリの入口。基本方針と記事一覧）" },
  { href: "/geo", label: "GEOとは（カテゴリの入口。AI検索対策の基本方針と記事一覧）" },
  { href: "/glossary", label: "用語集（SEO・GEOの用語を1語1文で定義）" },
  { href: "/learn", label: "SEO・GEOの教科書（14レッスン）" },
  { href: "/tools/page-audit", label: "ページ診断（URLを入れてSEO・GEOの指摘を出す自作ツール）" },
  { href: "/tools/prompt-fit", label: "プロンプト適合度（AI検索の想定質問にページが答えられているか調べる自作ツール）" },
];

/**
 * リンク先の一覧を作る。記事は draft と置き換え済み（noindex）を除いてから、新しい順に maxArticles 件。
 * category を渡すと同カテゴリを優先する（HOW TO は題材のカテゴリが先に決まっているため）。
 */
export function linkTargets({ category, maxArticles = 12 }: { category?: string; maxArticles?: number } = {}): LinkTarget[] {
  const lessons = [...LESSONS]
    .sort((a, b) => a.order - b.order)
    .map((l) => ({ href: `/learn/${l.slug}`, label: `教科書レッスン${String(l.order).padStart(2, "0")}「${l.title}」: ${l.goal}` }));

  const indexable = new Set(indexableArticles().map((a) => a.slug));
  const articles = getAllArticles()
    .filter((a) => !a.draft && indexable.has(a.slug))
    .sort((a, b) => {
      if (category && a.category !== b.category) return a.category === category ? -1 : b.category === category ? 1 : 0;
      return a.date < b.date ? 1 : -1;
    })
    .slice(0, maxArticles)
    .map((a) => ({ href: `/articles/${a.slug}`, label: a.title }));

  return [...FIXED_TARGETS, ...lessons, ...articles];
}

/** userPrompt の末尾に付ける一覧。中身が実行ごとに変わるので SYSTEM_PROMPT には入れない（キャッシュを壊さない） */
export function linkTargetList(targets: LinkTarget[]): string {
  return `# リンク先の一覧（内部リンクはこの中からだけ選ぶ）
${targets.map((t) => `- ${t.href} : ${t.label}`).join("\n")}`;
}

export const AUTHOR_RULES = `# 書き手についての制約
- 書き手個人の経歴・前職・実務経験には一切触れない。「〜の経験から言うと」「私が〜で見てきた」のような
  一人称の経験談を書かない。分析の根拠は一次情報と公開情報だけにする。`;

/**
 * 文体ルール。文字数とFAQの上限問数だけ記事の型ごとに変える。
 * FAQは**任意**（`faq` は上限で、下限ではない）。AI検索が抜き出す単位は見出しとその直下のブロックで、
 * FAQ節に固有の効果は測られていない（FAQPage のリッチリザルトは2026-05-07にGoogle検索から廃止。
 * Ahrefsの1,885ページ調査でも構造化データの追加でAI引用は増えていない）。
 * 本文の言い換えになる問いを足すより、各見出しの直下を自己完結させるほうに字数を使う。
 */
export function styleRules({ chars, faq }: { chars: string; faq: string }) {
  return `# 文体
- です・ます調。一文は60字以内。1段落は3文以内。
- 次の表現は使わない: 「〜と言えるでしょう」「〜が重要です」「〜に注目が集まっています」「本記事では」「この記事では」
  「いかがでしたか」「まとめると」「〜と言っても過言ではありません」「〜することが求められます」。
- 「重要」「注目」「最適化」を連発しない。代わりに具体的な動詞で書く（例: 「title を書き換える」「ログを見る」）。
- 見出しは ## と ### のみ。冒頭は「## 結論」で、何が起きて何をすべきかを2〜3文。
- **「## 結論」の1文目は、記事タイトルの問いにそのまま答える断定文にする**（主語＋何がどうなのか。60字以内、
  固有名詞を含める）。前置き・背景説明から始めない。AI検索と強調スニペットはこの1文を抜き出す。
- **「## 結論」の中で、読者が今やっていて今日やめるべきことを1つ名指しする**
  （例:「1回の回答を見て『AI順位3位』と報告してはいけません」）。事実の紹介だけで結論を終わらせない。
- 文字数は${chars}。
- **各 ## の直下の最初の段落は、見出しを読まなくても意味が通る1〜3文にする**
  （AI検索が抜き出す単位は見出しとその直下のブロック。FAQ節だけが抜き出されるわけではない）。
- 末尾の「## よくある質問」は**任意**。本文のどの見出しでも答えていない質問が実在するときだけ、${faq}置く
  （### に質問文、直下に回答）。本文に書いたことの言い換えになる問いは置かない。1問も無くてよい。
- **FAQを置くなら、回答は質問文を読まなくても意味が通る1〜3文にする**（AI検索は回答だけを抜き出す）。
  「はい」「できません」だけで終わらせず、何がどうなのかを回答文の中で言い切る。
- 本文中に出典URLを再掲しない。
- **太字は記事全体で10箇所まで**（機械検査あり）。段落ごとに太字を付けない。1つのセクションで一番効く文だけに付ける。
- **段落まるごとを太字1文にしない**（機械検査あり）。前の段落を言い直すだけの決め台詞になる。
  短い段落を置くのは、新しい事実を足すときだけにする。
- **「Xではなく、Yです」で強調しない**。誰も主張していない否定を先に置くと、後半が実際より大きく聞こえる。
  読者が実際に思い違いをしている場合だけこの形を使い、それ以外は「Yです」と直接書く。
- **本題の前に前置きを置かない**。「先に〜を書いておきます」「ここから見ていきます」「結論から言うと」は消して、
  その中身をそのまま書く。断り書きは、本題の前に理由だけを置く。
- **箇条書きの各項目に太字ラベルを付けない**。「**速度:** 速度が上がりました」のように、
  ラベルと本文が同じことを言う形にしない。ラベルが無くても読めるなら地の文で書く。
- **装飾の矢印（→）を文章の中で使わない**。「AがBになり、そこでCになる」と語で書く。
  図解コンポーネントのラベルは対象外。
- **英語のまま書かない**。出典が英語でも、用語・機能名・レポート名は日本語にする
  （例: classic Search→通常の検索結果、site owner→サイト運営者、Performance report→検索パフォーマンスレポート）。
  日本語の定訳が無い固有名詞（AI Overviews、Search Console など）はそのままでよい。
  原文の引用は FigureQuote の中だけに置き、図の直後の段落で日本語の意味を書く。`;
}
