# Figmaのデザインシステム化

`src/app/globals.css` のトークンを正として、Figma側に変数・スタイル・コンポーネントを作る。

## 決めたこと

- **ローカルプラグイン（`figma-plugin/`）を正の手段にする**。MCP（Figmaのリモート実行）は同じ Plugin API の
  JSを外から流し込む仕組みだが、プランごとのツールコール上限がある。2026-09-21 に Starter の上限に当たり、
  トップページの組み立てが1回目の書き込みで中断した。ローカルプラグインなら実行回数は無制限。
- **ライト/ダークは別コレクション**。無料プランは1コレクション1モードまで（`addMode` が
  `Limited to 1 modes only`）。Professional以上に上げたら1コレクション2モードへ統合する。
- **画面（トップページ）のFigma化は後回し**。まずデザインシステム（変数・スタイル・部品）を土台として入れる。

## 経過

- [x] MCPでトークンを流し込み（2026-09-21）。4コレクション・34変数・影2スタイル。読み戻して検証済み
- [x] MCPのツールコール上限に到達 → ローカルプラグイン方式へ切り替え
- [x] 参照用スクリーンショットの取得（2026-09-22）。本番 `seo-geo-lab.com` は実行環境のプロキシに拒否されたため、
      `npm run build` → `out/` をローカルに配って撮影。トップ全体 1440×約8800px
- [x] `figma-plugin/`（manifest / code.js / README）を作成
- [x] `figma-plugin/smoke-test.mjs`: Plugin API のモックで `code.js` を実行。1回目＋2回目（冪等性）を検査。
      `npm run figma:check`／CIでも走る
- [x] `src/lib/figmaTokens.test.ts`: `globals.css` とプラグインのトークン表のズレを検出（`npm test`）
- [ ] **Figmaデスクトップアプリで実機実行**（人手。プラグインは実機で一度も動かしていない）
- [ ] 実機の見た目を確認して `Design Tokens` フレームの崩れを直す
- [ ] ライブラリとして公開（Figmaのプラン次第。Starterでは他ファイルへの公開ができない）
- [ ] トップページ（1440幅）をFigmaフレームとして組む。参照スクショと実測値は取得済み

## 実機実行で見るところ

モックは構造と呼び出し順しか見ていない。実機では次を目で確認する。

1. フォントの解決結果（和文が Noto Sans JP になっているか。無ければ Inter に落ちて和文が豆腐になる可能性）
2. `Design Tokens` フレームのスウォッチが、プレートの中に収まっているか（はみ出し・文字の切れ）
3. 半透明トークン（`line` / `fill` 系）が意図した薄さで出ているか
4. `Button` の4バリアントが1つのコンポーネントセットに入っているか
5. 2回目の実行で変数・スタイル・コンポーネントが増えていないか

## 測ったこと（トップページ、1440幅）

| 対象 | 値 |
|---|---|
| コンテンツ幅 | 1112px（`container-page` 1152 − 左右の px-5） |
| 記事カードのグリッド | 2カラム・gap 20px・1枚 546px |
| カードの角丸 | 24px（`radius-card`） |
| キービジュアル | 通常カードは 16:6、先頭の大きいカードは高さ 176px |
| ヒーロー | 常に黒地（`ink` / `paper`）。上下の余白 40px |
