# 自サイトがAI検索に引用されない原因の切り分け

「まだ全然引用されない」に対して、コードで確かめられるところと、確かめられないところを分ける。

## 分かっていること（実測）
- [x] 公開記事35本。うち `original: true`（独自の実測・検証が中心）は **1本**。残り34本は他媒体ニュースの日本語要約。
- [x] `/tools/page-audit` の検査ログ（Supabase `seogeo_audit_log`）は **0件**。ツールがほぼ使われていない＝流入がほぼ無い。
- [x] 技術面（構造化データ・llms.txt・canonical・sitemap・robots）は実装済み。ここにこれ以上足しても引用は増えない。
- [x] 本文抽出が回遊導線で汚れていた。関連記事カード・次に読む・カテゴリ記事一覧が `<section>` で本文と同じ扱いになり、
      記事25が「AI引用に効くschemaとは？」（別記事のカードのタイトル）で schema のプロンプトに満点で当たっていた。

## やったこと
- [x] `scripts/prompt-gap.ts` + `content/prompts.csv`: 狙うプロンプト28本に対して、ビルド済みHTMLのどのページがどれだけ
      答えているかを弱い順に出す（判定は `/tools/prompt-fit` と同じ `src/lib/promptFit.ts`）。
- [x] 回遊導線を要素で分離: `NextStep` を `<nav>`、記事下の関連記事と `CategoryArticles` を `<aside>` に。
      判定側 `blocksFromHtml` も `nav, aside, footer` を落とす。
- [x] 直答の追加2件（判定で「弱い／答えていない」と出たもののうち、既存の出典の範囲で書けるもの）
      - `/learn/geo-implementation` robots.txtの書き分け: 冒頭に「引用されたいなら検索用ボットは許可、
        学習に使われたくないなら学習用ボットだけ拒否。GPTBot拒否＝ChatGPTの検索から消える、ではない」を明記
      - `/learn/long-tail`: 読者が使う語「ロングテールキーワード」が本文に1度も無かったので冒頭と手順の章に入れた

## 残り（コードでは解決できない。運営者の判断が要る）
- [ ] **独自データを持つ記事**。要約記事はAIから見て一次情報（Google公式・SEJ等）の下位互換で、引用する理由が無い。
      自分で取ったログ・実測値（例: 自サイトのAIクローラーのアクセス記録、診断ツールの集計）を出す記事を増やす。
- [ ] **被リンク・言及**。AI検索は検索インデックスの候補集合から引く。候補に入らない限り、ページ内をいくら整えても引用されない。
- [ ] 出典が要る直答（下は判定で穴と出たが、一次情報が無いので書いていない）
      - タイトルタグの文字数（Googleは文字数を規定していない。書くなら出典を決めてから）
      - GEO対策の費用相場（金額の出典が無い）
      - 個人ブログのSEOで最初にやること（`/learn/first-week` はあるが「個人ブログ」の語で書かれていない）
